version: '3.9'

services:
  db:
    image: postgres:16-alpine
    env_file:
      - .env
    ports:
      - '5432:5432'
    volumes:
      - pgdata:/var/lib/postgresql/data

  worker:
    build: ./server
    env_file:
      - .env
    ports:
      - '9100:9100'

volumes:
  pgdata: 